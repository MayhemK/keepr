namespace keepr.Services;

public class ProfileService
{
  private readonly ProfileService _repository;
  public ProfileService(ProfileRepository repository)
  {
    // _repository = repository;
  }

}